.DS_Store
.gradle/
.kotlin/
build/
app/build/
local.properties
.idea/
*.iml
captures/
.externalNativeBuild/
.cxx/
.build/
*.xcodeproj/

# 보안 — 릴리즈 서명 키 (절대 커밋 금지)
*.jks
*.keystore
keystore.properties
# 보안 — HTTPS 자체서명 키스토어 비밀번호 (절대 커밋 금지)
tls.properties

# 보안 — TLS 재생성 산출물 (server.p12는 앱 assets 번들로 추적 유지)
*.pem
*.key
*.p12.tmp

# 스크린샷 — 개발 용도 (저장소 제외)
docs/screenshots/
