# PLAN v0.20 — 미리보기·검색·공유 (android)

> v0.19(외부 재생·자동 운영) 후속. 남은 제안 전 계열 마무리.

## 범위
- **A2 썸네일(T-949)**: FFmpegKit 1프레임 추출(영상 10초 지점, 320px) + `/.thumbs/` 캐시 + `GET /thumb/{name...}` + 보관함 목록 미리보기. 실패 시 아이콘 폴백.
- **B3 토렌트 검색(T-950)**: Jackett/Prowlarr Torznab API 연동(서버 URL+API키 설정, 검색→시드순, magnet/링크 바로 추가). 직접 스크레이퍼는 제외.
- **D1 만료 공유 링크(T-951)**: 토큰+유효시간(시간 단위) 발급(`POST /api/share`), `GET /s/{token}` 다운로드. 만료·삭제 API.
- **D2 게스트 읽기전용(T-952)**: 별도 게스트 비밀번호 → 보관함 열람·다운로드만(등록·삭제·설정 차단). 기존 BasicAuth 파이프라인에 역할 추가.
- **D3 위젯/퀵타일(T-953)**: 서버 시작/정지 토글 위젯(홈 화면) + 퀵세팅 타일.

## 검증 게이트 (T-954)
- `test` + `lint`(스크립트 제외) + `assembleDebug` GREEN → 실기기 E2E(썸네일 표시·검색→추가·공유링크 만료·게스트 차단·위젯 토글) → CHANGELOG + 세션 로그 + 커밋/푸시(요청 시).
